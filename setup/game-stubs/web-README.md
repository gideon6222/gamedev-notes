# {{NAME}}

{{DESCRIPTION}}

A phone game that runs in the browser and installs to the home screen. Every push to `main`
runs typecheck, golden tests, build and smoke, then deploys to GitHub Pages at
https://gideon6222.github.io/{{SLUG}}/ . The build stamp is in the menu.

```powershell
npm install ; npm test ; npm run build ; npm run e2e
```

Built with Claude Code on the process in `C:\dev\gamedev-notes` (`WEB.md`).
