# {{NAME}}

{{DESCRIPTION}}

A phone game that runs in the browser and installs to the home screen. Every push to `main`
runs typecheck, golden tests, build, the bundle size guard and the smoke test, then deploys
to GitHub Pages at https://gideon6222.github.io/{{SLUG}}/ . The build stamp is on screen.

```powershell
npm install ; npm run check
```

Built with Claude Code on the process in `C:\dev\gamedev-notes` (`WEB.md`).
