# The machinery, one line per knob

For the `studio-admin` skill. Every setting that governs how the studio is managed, where it
lives, and what it does. Read the line, open that file, change it, update this line if the
default moved.

## What runs when

- Every Claude Code session start: `setup\session-start.ps1` (hook in `~/.claude/settings.json`, installed from `setup\settings.merge.json` by `setup\install.ps1`). Pulls this repo, clears stale zero-byte `.git\index.lock` files under `C:\dev` older than 10 min, lists inbox lesson titles, prints the last weekly verdict from `reports\LATEST.txt`. Read-only apart from the locks.
- Every game commit: the game's `scripts\check.ps1` runs `doctor.ps1 -Repo <slug> -Quiet` last. Knowledge-base findings are WARN there, never blocking (`doctor.ps1`, function `Fail`).
- Weekly, Sunday 18:00 local: Windows Task Scheduler task "gamedev-notes weekly check" runs `scripts\weekly-check.ps1` (full doctor, writes `reports\`, no Claude). Registered by `setup\install-schedule.ps1`; `-Remove` removes, `-RunNow` fires it, `-Day`/`-At` move it.
- On demand: `/framework-check` (judgement on the doctor), `/digest` (fold the inbox), `/studio-admin` (this).
- At a milestone report and at every `/ship`: `scripts\progress.ps1` (`-Phase <active phase>`). Reads the game's `PLAN.md` checkboxes and prints where the plan stands; it writes nothing, spends no tokens, and no gate and no hook calls it. Its milestone regex is the same one as `doctor.ps1 Test-PlanOutline`, so a change to one is a change to both, and both carry a comment saying so.
- Nothing in Cowork runs on a schedule. The old Cowork weekly task was deleted on 2026-09-12; it burned Fable tokens, needed the PC linked, and left git locks behind.

## The dashboard (`C:\dev\studio-dashboard`, private site on Cloudflare Pages behind Access)

- Shows every robot (the machinery, one card each, defined in `agent\robots.json`), the rules log (inbox lessons by game, waiting or folded), activity, games, and the questions and suggestions Gideon sends from the site.
- How the dashboard works and how to change it: `C:\dev\studio-dashboard\CLAUDE.md` (loaded automatically by any Claude Code session started in that folder). Changes deploy when the Courier commits them, or at once with the site's run-now button.
- The Courier is three Task Scheduler tasks, registered by `agent\install-agent.ps1`. "studio-dashboard publish", every 2 min, `agent\publish.ps1`: collects stats (no tokens) and pushes only if something changed, because every push is a Cloudflare Pages build and the free plan allows 500 a month. "studio-dashboard agent", every 10 min and whenever GitHub moves, `agent\run.ps1`: handles `queue\*.json` (a question goes to Haiku, Sonnet or Opus by `Select-Model` in `agent\process-queue.ps1`, the reason is shown on the site; a suggestion is evaluated by Opus with `agent\prompts\suggestion-evaluate.md` and, if approved, implemented by Sonnet or Opus with `agent\prompts\suggestion-implement.md` in this repo and the template, never in a game repo, followed by a full doctor run), then the digest, then one publish. "studio-dashboard agent watcher", every 1 min: a git fetch that wakes the slow lane when main moved. The two lanes hold different mutexes, so a 40-minute petition no longer stops the page refreshing; the page shows what the slow lane is doing instead (`site\data\agent.json`). Knobs: `agent\install-agent.ps1 -PublishMinutes 2 -Minutes 10`; per-job ceilings in `agent\process-queue.ps1` are a wall clock (`-QuestionMinutes 8`, `-EvaluateMinutes 12`, `-ImplementMinutes 75`) **and** a turn count (`-QuestionTurns 60`, `-EvaluateTurns 60`, `-ImplementTurns 150`), raised in step or a turn rise just moves the kill to the clock.
- The urgent lane is `agent\urgent.ps1`, on demand. `agent\watch.ps1` starts it within a minute of the queue file appearing on main, and `agent\run.ps1` starts it too in case the watcher missed it. It holds `Global\studio-dashboard-urgent`, its own mutex, so a petition already in progress never delays it. Knobs: `-TickSeconds 30` (how often the card's progress line is rewritten) and the same two ceilings as the slow lane, `-EvaluateMinutes 12` / `-ImplementMinutes 75` and `-EvaluateTurns 60` / `-ImplementTurns 150`. The verdict is **always Opus**, urgent or not, and the model that implements is the one Opus names, so pressing Urgent buys a lane of its own and a progress line, never a cheaper model, a shorter leash or a smaller tool set.
- **An urgent request now costs no Cloudflare build at all.** It cost two until 23:02 on 2026-09-12 (the queue commit in, the Courier's removal commit out); since `0e77ff5` in the dashboard a request is its own ref, `refs/studio/queue/<id>`, written by the site function with the git-data API and mirrored onto the PC by `agent\queue-sync.ps1`, so nothing about a request touches main. The thirty-second progress ticks never cost one either, because `agent\publish.ps1 -NoCollect` force-pushes one commit object to `refs/studio/data`, a ref that is not a branch and is never built. Measured 2026-09-12: main carried 123 commits in September, 76 of them `Courier: stats` pushes, and those stopped at 21:44 that evening when the data moved off main. What is left that builds is a source commit, which is a person editing the site, so the free plan's 500 a month is no longer a function of how often he presses Urgent. The page's amber pill at 250 builds and red at 400 still watch the real total.
- The phone lane is `agent\install.ps1`, on demand, started by `agent\watch.ps1` (and by `agent\run.ps1` if the PC was asleep) when a queue file named `*-p-*.json` is waiting; `agent\process-queue.ps1` skips those the way it skips `-u-`. It is the Games tab's Check phone and Update buttons, and it **spends no tokens at all**: no `claude` call anywhere in it, no build from source, and nothing written into a game repo. `-Check` reads `adb shell dumpsys package` per game; `-Slugs a,b,c` downloads the release's `<slug>.apk` with `gh release download` into `C:\dev\studio-dashboard\build-cache\<slug>\<tag>\` (gitignored), reads its real versionCode and versionName with `C:\dev\toolchain\android-sdk\build-tools\36.1.0\aapt2.exe dump badging`, installs with `adb install -r -g`, and reads dumpsys back to confirm. Knobs: `-CheckMinutes 2`, `-InstallMinutes 10` (how long the phone is claimed), `-MaxSlugs 12`, `-Aapt2`, and `PHONE_FRESH_MIN` (10) in `site\index.html`, which is how old a reading may be before Update all stops trusting it. Its mutex is `Global\studio-dashboard-phone`, its data mutex `Global\studio-dashboard-phonedata`, and it appears on the Robots tab as the Porter (`source: log:porter`, cadence on demand, cost none).
- **The phone lane obeys the same lease every game session does** (`scripts\phone.ps1`, owner `porter`), takes it *before* it asks whether anything is plugged in, and releases it in a `finally`. Exit 75 is not an error: it writes "phone busy: <holder> has it" onto the page and stops without a single adb call. A lease still owned by `porter` when it starts belongs to a killed run and is cleared, the way each Courier lane clears its own knowledge-base lease. Presence is `scripts\phone.ps1 devices`, added for this and documented in `INDEX.md`: the one read-only adb action in the studio, no lease, nothing touched on the handset.
- A petition stopped by either ceiling has already written files. `claude -p` ends a turn-capped run with `subtype: error_max_turns`, exit 1 and a null result, which reads like a crash: on 2026-09-12 two petitions were reported as "Claude returned nothing" with every edit and every check done and only the commit left. The Courier now names the ceiling and lists the uncommitted paths and extra worktrees in the report, so the card points at the work instead of hiding it.
- The doctor runs before and after an implementation, and a petition is marked `implemented-with-fails` only when the FAIL count **went up**. A FAIL that was already standing is named in the report and left with whoever caused it. Before this, the filming petition was marked red for a cross-reference the Courier had broken an hour earlier.
- Feeds: `reports\robots.log` (one line per run, written by `session-start.ps1`, `weekly-check.ps1` and the Courier), git history here (Lesson:, Digest:, Admin: subjects), each game's `build\check-*.log`, `reports\LATEST.txt`.
- A suggestion implemented from the site shows up here as an `Admin:` commit, so name commits that way.

## Thresholds (all in `scripts\doctor.ps1` unless noted)

- Inbox backlog: PASS up to 10, WARN over 10, FAIL over 40 (`Test-InboxBacklog`). The documented digest trigger is ten (`INDEX.md`, `session-start.ps1`).
- Auto digest: the Courier runs `/digest` unattended with Sonnet (`C:\dev\studio-dashboard\agent\digest.ps1`) when the last Digest commit is old enough, no lease is held, and **either** the inbox is over 10 (`-Threshold`, then `-MinHours 2`) **or** a topic file has reached 34 KB (`-SizeKb`, then `-SizeMinHours 12`). The size trigger exists because condensing a topic file is something only `/digest` may do, so before it the cure for an oversized file was gated on an unrelated lesson backlog: on 2026-09-12 TESTING.md sat at 34.8 KB with eight lessons in the inbox and nothing scheduled would ever have brought it down. It gets the longer quiet period because folding lessons is not guaranteed to shrink a file and a two-hour window would re-fire for ever. A size run is judged a success on kilobytes removed, not on lessons folded. The report lands on the dashboard's Chronicle as a Librarian deed. Two places quote this trigger to build sessions and must move with it if it changes: the "What runs on its own" section of `INDEX.md` and the over-ten line in `setup\session-start.ps1`.
- Digest age: WARN when lessons are waiting and the last `Digest` commit is over 3 days old (`Test-DigestAge`).
- Lesson format (missing `Belongs in:` or `## Replaces or contradicts`): WARN, never FAIL (`Test-LessonFormat`).
- Topic file size: WARN over 32 KB, FAIL over 35 KB (`Test-TopicFileSize`). The written target is about 30 KB. The auto digest fires at 34 KB, which is the last KB before the FAIL.
- Game files in the knowledge base: WARN while `assets\` or `addons\` exists here (`Test-KbStrays`). Assets belong to the game that uses them. `scripts\assets.py` now refuses to fetch into this repo, so this catches what is already on disk and anything hand-copied.
- Template drift: WARN for any template script whose content differs, except names listed in the game's `scripts\DIVERGENCE.md` (`Test-TemplateScriptSet`).
- Dev states: WARN when a game's `src\game\*.gd` has no `dev_seek(name)` or no `dev_heartbeat()` (`Test-DevStates`). Never FAIL: a game that predates the seam is not broken, it is paying the old price, and only that game's own session can write the arms - the states are the GAME's, which is why they live there and not in the tool. Read from the stripped text, so a header quoting `dev_seek` is not mistaken for one. Every game repo warned on 2026-09-13; `godot-template` is the positive control and passes.
- Film watchdog: `-StallSeconds 45` and `-MaxMinutes 15` on each game's `scripts\movie.ps1` (not in `doctor.ps1`). Every film passes `beat` to the game, which answers with `DEVBEAT` lines from `Main.dev_heartbeat()` every 30 physics frames (`BEAT_FRAMES` in `scripts\replay_player.gd`). The stall clock watches the READING, not the line, so a game printing the same numbers is as stuck as one printing none; it is armed from launch so a hang before the first frame is caught, and it disarms itself when the game says it has no `dev_heartbeat()`, leaving the wall clock. After the run the first and last readings are compared and an unchanged pair throws. Raising `-Seconds` past a few minutes means raising `-MaxMinutes` with it or the film is killed at 15.
- Plan outline: WARN when a game's PLAN.md has no `- [ ]` milestone lines (`Test-PlanOutline`). The dashboard's checklist reads those boxes; the changelog tab reads `src\changelog.gd` or `.ts`.
- Phone debt: WARN while a game's NOTES.md holds a `PHONE TEST OWED` line (`Test-PhoneDebt`). Never FAIL: the repo cannot clear it while another game has the phone.
- Version name against the changelog: WARN when `version/name` in the presets is not the version the newest `src\changelog.gd` entry names (`Test-VersionCode`, the last branch before its PASS). That is the one condition which makes the dashboard's phone page lie, because it compares the versionName read off the handset with this number: when the two drift, a row reads "outdated" for a build that is already installed and no amount of pressing Update clears it. WARN and never a gate, because only that game's own next ship can fix it.
- Git hygiene: WARN on uncommitted or unpushed work, WARN on a large `.git` (`Test-GitHygiene`).
- US English (`INDEX.md` rule 15): WARN, never FAIL (`Test-USEnglish`). The word list is `scripts\us-english.txt`, one `british=american` pair per line, read once; add a word there and the check, `/digest` and the rule all learn it. In a game it reads the spacey double-quoted strings in `src\**\*.gd`, `text` and `tooltip_text` in a `.tscn`, every changelog line and README prose, skipping `addons\`, `assets\CREDITS.md`, `scripts\`, `build\` and `.godot\`. In the knowledge base it reads `INDEX.md`, `skills\*\SKILL.md` and `agents\*.md` and NOT the topic files, which `/digest` converts a section at a time as it folds lessons in (step 6 of its skill); a permanent 373-line WARN would just teach everyone to stop reading this report.
- Snapshot dirs (`*.pre-fix`, `*-audit`, `*.bak`, `*.old`) are skipped unless `-IncludeSnapshots`.
- Weekly reports kept: 8 (`weekly-check.ps1 -Keep`).

## Concurrency

- Phone lease: `scripts\phone.ps1` (`claim` / `release` / `status` / `devices`), lease file `C:\dev\.phone-lease`, fixed and outside every repo so it can never be staged. 20 minutes, renewed by every `device.ps1` command; 60 for a live `device.ps1 log`. A busy phone is exit code 75 from `phone.ps1` and from each game's `scripts\device.ps1`, which writes one `PHONE TEST OWED` line into the game's `NOTES.md` and touches no adb. Anything staler than its own `minutes=` is taken over, loudly. The dashboard's phone lane holds the same lease as owner `porter` (2 minutes to read, 10 to install) and queues behind a playtest like anything else. `devices` is the one read-only action and the one sanctioned bare `adb` call in the studio: it asks the local adb server what is attached, takes no lease, and changes nothing on the handset, because a page that asks "is it plugged in" on every load must not be able to take the phone away from a filming run.
- Digest lease: `scripts\kb.ps1`, `$LeaseMinutes = 45`. Lease file is `.git\kb-lease`, never tracked. A stale lease is taken over with a notice, never a block.
- One robot writes the knowledge base at a time. Both the urgent lane and the slow lane take the digest lease (`scripts\kb.ps1 lease`, as owner `urgent` and `courier`) around their implement step and release it in a `finally` that also covers the clock kill and the turn ceiling, so a petition, an urgent request and the auto digest queue behind each other instead of only behind themselves. The wait shows on the card ("waiting for the notes: digest holds the lease, 6 min left"), gives up after `-LeaseWaitMinutes 20` while looking every `-LeasePollSeconds 15`, and marks the request `needs-you` rather than starting a second writer. The **evaluate** step deliberately takes no lease: its tools are Read, Grep and Glob, so it writes nothing, and holding a 45-minute lease through a 12-minute read would block the Librarian for no reason. A run killed outright never reaches its release, so each lane clears a lease still owned by its own name before it starts, and the 45-minute staleness above covers whatever misses even that.
- The two lanes also share `Global\studio-dashboard-suggestions` around the read, the replace and the write of `site\data\suggestions.json`. Re-reading before the write is not enough on its own, because splitting the re-read from the write IS the race: measured 2026-09-12 with the window widened to three seconds, the unheld version destroyed 4 of the other lane's 40 records and the held version none.
- Naming files protects you from sweeping up *other* files, not from sharing one. `kb.ps1 commit` stages the names you give it whatever state they are in, so a file another session left dirty rides along in your commit. Observed 2026-09-12: `3bb8752` ("filming writes one MJPEG") carried the phone lease's edit to `skills\playtest\SKILL.md`, because both sessions were editing that file at once. Read `git status` for the names you are about to stage, not just for the ones you are leaving out.
- `kb.ps1 commit` refuses `-A`, globs, directories and pathspec magic. It accepts a tracked file that is no longer on disk as a deletion, and splits a comma-joined `-Files` string (the `-File` invocation binds it as one).
- Stale `index.lock` (zero bytes, over 10 minutes old): cleared by `session-start.ps1` and `weekly-check.ps1`. A Cowork chat running git against the mounted folder is the usual source; the project rules tell it not to.
- Branches: never `git checkout -b` in `gamedev-notes`; use `git worktree add`. (`CLAUDE.md`, `README.md`.)

## Skills (`skills\<name>\SKILL.md`, junctioned into `~/.claude/skills`, live on save)

game-studio (build loop and routing), new-game, game-plan, game-scaffold, asset-hunt, playtest, ship, record-lesson, digest, framework-check, studio-admin. Each under 300 lines.

## Agents (`agents\*.md`, copied into `~/.claude/agents` by `install.ps1`, so re-run it after a change)

doctor-runner (haiku), lesson-filer (haiku), game-researcher (sonnet), asset-scout (sonnet), playtester (inherit). Routing table and how to verify: `MODELS.md`.

## Permissions and hooks

`setup\settings.merge.json`: the allow list (git, gh, godot, adb, powershell, node, python and the read and edit scope `C:\dev`), the deny list (`C:\dev\keys`, `C:\dev\.env`, force push), `additionalDirectories`, and the SessionStart hook. `install.ps1` merges it into `~/.claude/settings.json`; it does not remove entries it did not add. If Claude Code prompts for a command that should be routine, add its pattern to `allow` and re-run `install.ps1`.

## Where the game side is documented

`INDEX.md` (loaded in every session), `README.md` (file map and game table), the topic files. `AUDIT-2026-09-11.md` and `AUDIT-PROGRESS.md` are history; read them only when a question is about why a rule exists.
