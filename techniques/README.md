# techniques/

Deep technical write-ups pulled out of `CRAFT.md` and out of the old single pipeline file, archived on 2026-09-09 as `archive/PIPELINE-2026-09-09.md` and `WEB.md`. Most files here are game-specific; a few (`generated-audio`, `human-bot-policies`, `three-js-traps`, `phone-camera-and-light`, `diagnosis-and-rework`, `filming-a-run`, `pixel-and-model-assertions`, `phone-layout-and-safe-area`, `build-output-and-the-package`) are cross-game. One file per technique. Each starts with the game, its status, a "read when" trigger, a one-paragraph intro and two or three generalisable takeaways; the body is the original text, lightly edited for standalone reading. The general rules distilled from these live in the topic files — come here when you are about to build the same kind of thing and want the arithmetic, the constants and the dead ends.

| File | What it is | Read when |
|---|---|---|
| `coreward-propagated-lighting.md` | Coreward's lamp: a flood fill over open cells stored as a tiny light-field texture, per-pixel falloff in the shader, a grid-DDA ray fan for corner shadows, separate surface and air light, the additive tunnel-glow quad, and every artefact those produced (shadow acne, one-cell bleed, the sRGB grey wash, the dimmer-switch fix, world-anchored dust). | A light has to respect geometry on a grid; a "circle of light" or "angled shadow" artefact; a lighting complaint that survives a correct fix; ambient particles that read as dirt on the lens. |
| `wrecking-crew-pendulum.md` | The ball-and-chain: radial/tangential constraint, `g * offset / length`, period solved from a feel constant, quarter- vs half-period load (4.19 vs 6.76), velocity-lerp rig, alignment cone and the 45% throttle floor, tank controls under a fixed camera, the dial-then-slider control, saturation instead of an energy ceiling. | Any indirectly driven tool (pendulum, trailer, wind-up); a tracked or steered vehicle under a fixed camera; a physics constant that must be tuned for a thumb rather than real; a "drifts" or "no momentum" report. |
| `stillwater-fishing-fight.md` | The fight's three designs and why each failed: threshold → pump rhythm → tap-to-hold, with the surge, the 46 s wear clock, the nibble that teases (0.34 vs 1.0 depth), the one-knob species table (`win ~ 1.06 - 1.15 * run_chance`), the depth-driven four-bed crossfade, the sky series, swell-displaced water, heightfield depth instead of `DEPTH_TEXTURE`, post-processing under the HUD. | Designing a tension or hold-the-needle minigame; a difficulty table where "harder" and "more often" are the same number; a mood that must change over hours; water that facets; a depth-buffer read you cannot test. |
| `candle-gift-reference-runner.md` | Rebuilding a commercial runner from footage: guide-for-verbs, longest playthrough, 4x crops; the trailing tray along a recorded path with per-candle recipes; pools on the ground; ROTATE as a wall; hazards in one colour family; par from committed bots over several seeds; money in reference units; the run-vs-bank invariance test; pixel vs model checks; the contact sheet; the fresnel rim; why it was rewritten. | Copying an existing game; a formation that follows the player; runway stations and pickups; star thresholds and par; an economy that has to match a reference; a game that keeps inheriting a shape it never wanted. |
| `coreward-shop-room-and-hud.md` | The shop as a second scene the player stands in, the real ship reparented into it, portrait FOV arithmetic (46° vertical ≈ 22° horizontal), labels sized by pixels (0.66 units ≈ 70 px), availability said three ways and ordered by test; HUD readouts beside the thumb, SVG `pathLength` gauges, needles with mass, the rolled-steel plate and grain, pressed-in buttons, "unavailable" vs "not a control". | A shop or menu that still reads as a pop-up; laying out 3D for a phone in portrait; sizing labels, gauges and controls over a textured world; a row that is grey for the wrong reason. |
| `generated-audio.md` | Synthesising everything: Web Audio at runtime on the web stack, `AudioStreamWAV` at boot and offline-generated committed WAVs on Godot (22050 Hz mono; nineteen sounds in 4.0 MB; +3% APK); round-robin players; vertical layering on one scheduler; fade times that differ; alarm routing past the lowpass; the drone-vs-cadence composition lesson; the depth crossfade and its monotonicity test. | Adding sound or music; a generated bed that sounds like beeps or like dread; a score that must follow game state; deciding whether to import a sample; a music toggle with nothing behind it. |
| `human-bot-policies.md` | Scripted players with human faults: 300 ms reaction, a misread one time in six, a ±0.055 thumb, and memory that lags reality; model the input device (a tap rate corrected on a ~0.3 s cadence); measure claims where they hold; per-item landing rates; spread before thresholds; several seeds; sweep the bot's parameter before the game's; one definition of playing well shared by bots and tests; every policy failing differently. | Setting any balance constant, par or threshold; a bot that beats a mechanic every time; two policies scoring the same; a reading bot losing to a blind one; tests that play the game with their own helper. |
| `gravewell-tunnel-light-and-beam.md` | Lighting a dug tunnel: ambient that belongs to the passage rather than to the lamp (the propagated flood used RAW, with no distance term of its own), a beam given a profile ACROSS itself with a cubed falloff, the air-density scaling, and the ratio measurements that settled "directional but you can still see the way home". | A "circle of light that follows me" report; a beam that renders as a flat slab or has a visible end; deciding what number a lighting complaint is actually about. |
| `gravewell-continuous-digging.md` | Turning a per-cell drill into a continuous plow: chunky as a CADENCE rather than a curve, speed derived from the bill (`power / (hardness * HP per metre)`), depenetration while the hull is inside material, one `dig_load` driving four channels, the four faults the aggregate mean hid, and the character-width-to-cell-width ratio table that decides what granularity reads. | Making any discrete verb continuous; a "chunky", "rigid" or "it just drives through" report; a feel parameter that reads zero where the player starts; destruction that does not match how the terrain is drawn. |
| `coreward-authored-rooms.md` | Hand-written room templates stamped into a seeded world: rooms as drawings in the source, stamp order against singletons and generators, dropping colliding rooms whole, a third of them empty, the locked-door flood test, and the fog-of-war map drawn as ruled squares with a surveyed percentage (plus the three versions of the test that proved it drew). | Authored mystery in a generated world; a map panel that reads as a rendering bug; a pixel assertion that passes with the layer deleted. |
| `phone-camera-and-light.md` | The camera and lighting rules that used to be two `CRAFT.md` sections: portrait FOV arithmetic and px-per-world-unit at each depth, the derived first-person rig and peak angular rate, the framing solve, inverted follows, propagated light, beam shape, PCF on a shadow fan, ambient, post. | Placing anything in a portrait 3D scene; a camera that makes motion read wrong; any lighting complaint. |
| `diagnosis-and-rework.md` | What to do when a fix does not take: the correct-fix rule, the missing degree of freedom, rules that must become locations, masks, rewrites, and the filename-as-interface process rules. | A complaint survives a fix; a symptom that reads as tuning; renaming or moving anything in a game repo. |
| `three-js-traps.md` | The web-stack reference list: `InstancedMesh` and `setColorAt`, `rotation.order`, CSS sky vs `EffectComposer`, PMREM env maps and `colorSpace`, `MeshToonMaterial`, `FogExp2`, `vNormalMapUv`, `PlaneGeometry` facing, `Object3D.layers` not filtering lights (backwards in Godot), missing uniforms, `onBeforeCompile` chaining and `clone()`, chase-camera mirroring and the NDC test, `updateMatrixWorld`, second `Scene`, rAF undo, the tick seam, `localStorage` + `visibilitychange`, `Math.imul`, the preview service worker. One general lesson per item. | Working on a three.js or browser build; porting a web-stack rule to Godot and wondering whether it still applies; something renders as nothing or half a feature works. |
| `wildform-creature-shader.md` | Shading an IMPORTED model: why a uniform consumed in model space is tuned off `mesh.get_aabb()` and not world scale (`26.0 * model_scale` gave 0.3-1.7 bands, `4.0 / aabb.length()` gives 4.0 on every model), why a soft fresnel reads as panels on flat-shaded low poly and wants power 4.5+, and the `flat` and `pattern` debug render modes that belong permanently in the screenshot script. | Writing a shader for an imported model; a pattern that reads as uneven lighting; a rim that reads as a patchwork; a shader whose output is wrong and whose code looks right. |
| `wildform-evolution-transform.md` | Making a character visibly BECOME something else: why blend shapes cannot morph between two authored models at all, the dissolve/flash/reform swap with both forms frozen at a matching rest pose, and the tween whose length was a property of the content table (0.56 s ran 8.96 s) plus the `custom_step` measurement that catches it. | A character that has to change identity on screen; reaching for blend shapes; any animation whose length is set by a loop over a collection. |
| `build-output-and-the-package.md` | Build output written inside the project: the 1.42 GB APK, the 1.7 GB import cache and the 80 s import step, `exclude_filter` against `build/.gdignore`, the `build/*` then `!build/.gdignore` form, and why no scaffolded game ever had the marker. | Setting up a new repo's export and ignore guards; a game that has started filming runs; an APK or an import step that has quietly grown. |
| `phone-layout-and-safe-area.md` | The safe area on a real phone: `get_display_safe_area()` returning the usable DESKTOP off a phone (Gravewell 104 px up, Wildform an outset of -1460), the clamp that makes an inset non-negative by construction, and why headless cannot catch a layout bug by reading live values - put the arithmetic in a pure function and assert that. | Placing anything in a `CanvasLayer`; turning `screen/edge_to_edge` on; a control reported as half an inch too high; writing a test meant to protect a layout. |
| `pixel-and-model-assertions.md` | Which instrument for which bug: bounds not origins and a margin not a boundary, the picture with pixels and the placement with the model, non-linearity near the lens, and the three versions of "did the map layer draw" before one of them could fail. | Writing any check that reads a rendered frame; a layout assertion that passes while the screenshot looks wrong. |
| `filming-a-run.md` | Movie Maker, `replay_player.gd` and the bare-word `replay=`/`record=` flags, replay coordinates in the project viewport (1080x2338) rather than the window, `rects.gd` as the only probe that can produce them, the build stamp as a test subject, the wrapper that dies after writing every frame, and 150 MB per second of film. | Setting up filming in a new repo; a contact sheet that looks like the idle game; recording or writing a replay; a filmed run that fails after doing all the work. |
